# set default task and default training config based on task
defaults:
  - _self_ 
  - task: meta-world-v2 # load cfg/tasks/meta-world-v2.yaml
  - override hydra/job_logging: disabled

# Task name - used to pick the class to load
task_name: ${task.name}
# experiment name. defaults to name of training config
experiment: ''
# final run name
run_name: ''

# if set to positive integer, overrides the default number of environments
num_envs: ''

# seed - set to -1 to choose random seed
seed: 42
# set to True for deterministic performance
torch_deterministic: False

# set the maximum number of learning iterations to train for. overrides default per-environment setting
max_iterations: ''

## Device config
#  'physx' or 'flex'
physics_engine: 'physx'
# whether to use cpu or gpu pipeline
pipeline: 'gpu'
# device for running physics simulation
sim_device: 'cuda:0'
# device to run RL
rl_device: 'cuda:0'
graphics_device_id: 0

## PhysX arguments
num_threads: 4 # Number of worker threads per scene used by PhysX - for CPU PhysX only.
solver_type: 1 # 0: pgs, 1: tgs
num_subscenes: 4 # Splits the simulation into N physics scenes and runs each one in a separate thread

# set to True to use multi-gpu training
multi_gpu: False

capture_video: False
capture_video_freq: 1464
capture_video_len: 100
force_render: True

# disables rendering
headless: False

# set the directory where the output files get saved
hydra:
  output_subdir: null
  run:
    dir: .
  job:
    chdir: true

# used to overwrite task id for single task training
task_id: [0]
task_counts: [4096]
init_at_random_progress: True
exempted_tasks: []
record_videos: False
termination_on_success: False
reward_scale: 1.0

sparse_reward: False

fixed: True # set to True to fix the object and target positions across resets i.e the 'fixed' setting
same_init_config_per_task: False
